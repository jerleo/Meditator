ChatGPT:
write an android app in kotlin that sounds a chime every few minutes. the remaining time to the next chime should be counted down and displayed. the number of minutes can be selected with a slider between 2 and 30 minutes, the default being 5 minutes. a stop button at the bottom of the screen should cancel the timer. the default notification sound be used as chime. the app needs to ensure that a default sound has been set and notify the user otherwise. the name of the default notification should be displayed and refreshed when changing the slider.